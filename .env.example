# Platform database (Neon)
DATABASE_URL=
POSTGRES_URL=
POSTGRES_PRISMA_URL=

# Auth
JWT_SECRET=

# Neon API — required for auto-provisioning dedicated tenant databases on trial approve
NEON_API_KEY=
NEON_PROJECT_ID=
NEON_BRANCH_ID=
# Optional — defaults to the username in DATABASE_URL
NEON_DB_OWNER=neondb_owner

# Multi-tenant routing
NEXT_PUBLIC_ROOT_DOMAIN=3boxeshrms.com

# Site mode: live | demo (optional override)
SITE_MODE=
NEXT_PUBLIC_SITE_MODE=
